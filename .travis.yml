language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t bkhandruk/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bkhandruk/docker-react npm run test
